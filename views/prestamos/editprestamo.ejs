<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <title>Editar Item</title>
    <style>
        body {
            background: rgb(255,69,69);
            background: linear-gradient(90deg, rgba(255,69,69,1) 0%, rgba(255,156,115,1) 35%, rgba(252,245,150,1) 100%);
        }
        .card {
            margin: 0 auto; /* Centrar el card */
            float: none;
            margin-top: 50px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card bg-light mb-3" style="max-width: 50rem;">
            <div class="card-header">
                Modificar Préstamo
            </div>
            <div class="card-body">
                <h1 class="mb-4">Modificar Préstamo</h1>
                <form action="/prestamos/edit/<%= prestamo._id %>" method="POST"> <!-- La acción se dirige a '/prestamos/edit/:id' -->
                    <div class="form-group">
                        <label for="libro">Libro</label>
                        <input type="text" class="form-control" name="libro" id="libro" value="<%= prestamo.libro %>" required> <!-- Campo para el libro -->
                    </div>
                    <div class="form-group">
                        <label for="usuario">Usuario</label>
                        <input type="text" class="form-control" name="usuario" id="usuario" value="<%= prestamo.usuario %>" required> <!-- Campo para el usuario -->
                    </div>
                    <div class="form-group">
                        <label for="fecha_devolucion">Fecha de Devolución</label>
                        <input type="date" class="form-control" name="fecha_devolucion" id="fecha_devolucion" value="<%= prestamo.fecha_devolucion.toISOString().split('T')[0] %>" required> <!-- Campo para la fecha de devolución -->
                    </div>
                    <div class="form-group">
                        <label for="estado">Estado</label>
                        <select class="form-control" name="estado" id="estado" required>
                            <option value="activo" <%= prestamo.estado === 'activo' ? 'selected' : '' %>>Activo</option>
                            <option value="finalizado" <%= prestamo.estado === 'finalizado' ? 'selected' : '' %>>Finalizado</option>
                        </select> <!-- Campo para seleccionar el estado del préstamo -->
                    </div>
                    
                    <button type="submit" class="btn btn-primary">Guardar Cambios</button>
                    <a href="/prestamos" class="btn btn-secondary">Cancelar</a> <!-- Enlace para cancelar la edición -->
                </form>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
