<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <title>Editar Item</title>
    <style>
        body {
            background: rgb(255,69,69);
            background: linear-gradient(90deg, rgba(255,69,69,1) 0%, rgba(255,156,115,1) 35%, rgba(252,245,150,1) 100%);
        }
        .card {
            margin: 0 auto; /* Centrar el card */
            float: none;
            margin-top: 50px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card bg-light mb-3" style="max-width: 50rem;">
            <div class="card-header">
                Modificar Usuario
            </div>
            <div class="card-body">
                <h1 class="mb-4">Modificar Usuario</h1>
                <form action="/usuarios/edit/<%= usuario._id %>" method="POST">
                    <div class="form-group">
                        <label for="nombre">Nombre</label>
                        <input type="text" class="form-control" name="nombre" id="nombre" value="<%= usuario.nombre %>" required> <!-- Cambié 'animal' a 'usuario' -->
                    </div>
                    <div class="form-group">
                        <label for="apellido">Apellido</label>
                        <input type="text" class="form-control" name="apellido" id="apellido" value="<%= usuario.apellido %>" required> <!-- Cambié 'animal' a 'usuario' -->
                    </div>
                    <div class="form-group">
                        <label for="email">Correo</label> <!-- Cambié 'Correo' a 'email' -->
                        <input type="email" class="form-control" name="email" id="email" value="<%= usuario.email %>" required> <!-- Cambié 'animal' a 'usuario' y 'Correo' a 'email' -->
                    </div>
                    <div class="form-group">
                        <label for="telefono">Teléfono</label> <!-- Agregué el campo teléfono -->
                        <input type="text" class="form-control" name="telefono" id="telefono" value="<%= usuario.telefono %>" required> <!-- Cambié 'animal' a 'usuario' -->
                    </div>
                    
                    <button type="submit" class="btn btn-primary">Guardar Cambios</button>
                    <a href="/usuarios" class="btn btn-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
